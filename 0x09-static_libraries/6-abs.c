#include "main.h"

/**
 * _abs - Computes the absolute value of an integer.
 * @a: input number as an integer.
 * Return: absolute
 */

int _abs(int a)
{
	if (a >= 0)
	{
		return (a);
	}
	else
	{
		return (-a);
	}
}
